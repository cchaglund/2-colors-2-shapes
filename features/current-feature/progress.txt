## GALLERY-001 - getGalleryView() URL parser
- Added getGalleryView() to src/utils/urlParams.ts
- Returns { tab?: string } when ?view=gallery, null otherwise
- Follows same pattern as getProfileView(), getWinnersDayView() etc.
- Files changed: src/utils/urlParams.ts
- Pre-existing lint error in useCalendarChallenges.ts (unrelated)
- Next: GALLERY-002 (page component) or GALLERY-007 (App.tsx routing) — both depend on this

## GALLERY-002 - GalleryPage.tsx full-page component
- Created src/components/GalleryPage.tsx as standalone page (not modal)
- Follows existing page pattern: min-h-screen, max-w-4xl, back-to-app link, title
- Moved all data-fetching/state logic from Calendar.tsx (submissions, rankings, winners, challenges, month nav)
- Tab state owned internally; initial tab from URL prop; defaults to my-submissions (logged in) or winners (logged out)
- Tab switching updates URL via history.replaceState() — no full page reload
- Day clicks use same-tab navigation (window.location.href) not window.open
- Reuses all Calendar sub-components: CalendarViewToggle, ContentNavigation, CalendarGrid, CalendarDayCell, CalendarStats, WallTab, FriendsFeedTab
- Files changed: src/components/GalleryPage.tsx (new)
- Pre-existing lint error in useCalendarChallenges.ts still present (unrelated)
- Next: GALLERY-007 (wire into App.tsx routing) then GALLERY-003 (tab nav already partly done here)

## GALLERY-007 + GALLERY-014 - App.tsx routing + FollowsProvider
- Imported getGalleryView and GalleryPage in App.tsx
- Added galleryView useMemo check alongside other URL view parsers
- Renders GalleryPage in "standalone pages that don't need challenge data" section (before loading spinner)
- Wrapped in FollowsProvider (same pattern as UserProfilePage, FriendsFeedPage) — satisfies GALLERY-014
- Files changed: src/App.tsx
- Typecheck: pass. Tests: 102/102 pass. Lint: pre-existing error only.
- Next: GALLERY-003 (tab nav partially done in GALLERY-002), GALLERY-008 (toolbar nav), GALLERY-010/011 (cleanup)

## GALLERY-003, GALLERY-006, GALLERY-009 - Already implemented
- All three were fully implemented in GALLERY-002's GalleryPage.tsx
- GALLERY-003: tab from URL → initial state, internal state mgmt, replaceState on switch — all done
- GALLERY-006: back-to-app <a href="/"> link present in page header
- GALLERY-009: effectiveViewMode defaults winners when !user; CalendarViewToggle disables my-submissions/friends when !user
- No code changes needed, just marked as passing

## GALLERY-008 + GALLERY-011 - Toolbar gallery nav + remove onOpenCalendar
- Gallery button in Toolbar changed from <button onClick> to <a href="/?view=gallery">
- Removed login gate — gallery accessible to all users, no LoginPromptModal for 'submissions' variant
- Removed onOpenCalendar prop from ToolbarProps interface and destructuring
- Removed onOpenCalendar prop from App.tsx Toolbar usage
- Cleaned up loginModalVariant type: removed 'submissions' variant
- Files changed: src/components/Toolbar.tsx, src/App.tsx, features/current-feature/PRD.json
- showCalendar state + Calendar modal still in App.tsx — dead code, GALLERY-010 will clean up
- Typecheck: pass. Tests: 102/102 pass. Lint: pre-existing error only.
- Next: GALLERY-010 (delete Calendar.tsx modal), GALLERY-004 (thumbnail <a> links), GALLERY-012/013 (same-tab nav)

## GALLERY-004 - Thumbnail clicks use <a href> for same-tab navigation
- All clickable elements now use <a href> instead of onClick + window.location.href
- SubmissionThumbnail: added `href` prop; renders <a> when href provided, <button> when onClick, plain div otherwise
- CalendarDayCell: added `href` prop (onClick now optional); clickable cells render <a> when href given
- GalleryPage: replaced handleDayClick callback with getDayHref; passes href to CalendarDayCell
- WallContent/FriendsFeedContent: replaced handleThumbnailClick with getSubmissionHref; passes href to SubmissionThumbnail (falls back to onClick when onSubmissionClick prop is provided by other callers)
- Ctrl/Cmd+click now works correctly to open in new tab on all gallery navigation
- Files changed: SubmissionThumbnail.tsx, CalendarDayCell.tsx, GalleryPage.tsx, WallContent.tsx, FriendsFeedContent.tsx
- Typecheck: pass. Tests: 102/102 pass. Lint: pre-existing error only.
- Next: GALLERY-005 (back button), GALLERY-010 (cleanup), GALLERY-012/013 (same-tab nav in other pages)

## GALLERY-010 - Delete Calendar.tsx modal + clean up App.tsx
- Deleted src/components/Calendar/Calendar.tsx
- Removed Calendar import from App.tsx
- Removed showCalendar state from App.tsx
- Removed Calendar modal rendering + FollowsProvider wrapper from App.tsx
- Updated Calendar/index.ts: removed Calendar + CalendarProps exports (dead code)
- All sub-components (CalendarGrid, CalendarDayCell, CalendarViewToggle, etc.) remain in Calendar/ folder
- CalendarHeader.tsx is now dead code (modal-only close button) but kept per PRD scope
- Files changed: Calendar/Calendar.tsx (deleted), Calendar/index.ts, App.tsx
- Typecheck: pass. Tests: 102/102 pass. Lint: pre-existing error only.
- Remaining: GALLERY-005 (back button), GALLERY-012/013 (same-tab nav in other pages)

## GALLERY-012 - UserProfilePage same-tab navigation
- Changed window.open(url, '_blank') to window.location.href in handleSubmissionClick
- One-line change in UserProfilePage.tsx line 112
- Files changed: src/components/UserProfilePage.tsx
- Typecheck: pass. Tests: 102/102 pass. Lint: pre-existing error only.
- Remaining: GALLERY-005 (back button), GALLERY-013 (WinnersDayPage same-tab nav)

## GALLERY-013 - WinnersDayPage same-tab navigation
- Changed window.open(url, '_blank') to window.location.href in handleViewSubmission
- One-line change in WinnersDayPage.tsx line 36, same pattern as GALLERY-012
- Files changed: src/components/WinnersDayPage.tsx
- Typecheck: pass. Tests: 102/102 pass. Lint: pre-existing error only.
- Remaining: GALLERY-005 (back button) — last item

## GALLERY-005 - Browser back button returns to correct gallery tab
- Added useEffect to sync effectiveViewMode into URL via replaceState on mount/tab change
- Previously, URL only updated on explicit tab click (handleSetViewMode); default tab wasn't reflected in URL
- Now: even if user lands on default tab and immediately clicks a day cell, the URL has the tab param → back works
- All gallery navigation uses <a href> (full page loads), so back button triggers fresh page load with correct URL
- Files changed: src/components/GalleryPage.tsx
- Typecheck: pass. Tests: 102/102 pass. Lint: pre-existing error only.
- ALL PRD ITEMS COMPLETE
